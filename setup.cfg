[tool:pytest]
testpaths = tests
asyncio_mode = auto

[coverage:run]
source = custom_components/never_dry
omit = custom_components/never_dry/__pycache__/*

[coverage:report]
show_missing = true
fail_under = 60
